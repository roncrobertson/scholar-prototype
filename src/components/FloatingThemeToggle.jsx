import React from 'react';

/**
 * Theme toggle removed — app is light mode only.
 * Component kept so any existing imports do not break.
 */
export function FloatingThemeToggle() {
  return null;
}
